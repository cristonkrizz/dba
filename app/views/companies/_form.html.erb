<%= form_with(model: company, local: true) do |form| %>
  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :company_name %>
    <%= form.text_field :company_name %>
  </div>

  <div class="field">
    <%= form.label :profile_description %>
    <%= form.text_field :profile_description %>
  </div>

  <div class="field">
    <%= form.label :business_stream_id %>
    <%= form.text_field :business_stream_id %>
  </div>

  <div class="field">
    <%= form.label :establishment_date %>
    <%= form.datetime_select :establishment_date %>
  </div>

  <div class="field">
    <%= form.label :company_website_url %>
    <%= form.text_field :company_website_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
