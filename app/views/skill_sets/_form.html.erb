<%= form_with(model: skill_set, local: true) do |form| %>
  <% if skill_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(skill_set.errors.count, "error") %> prohibited this skill_set from being saved:</h2>

      <ul>
      <% skill_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :skill_set_name %>
    <%= form.text_field :skill_set_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
